# Mock recognition provider

The mock provider returns a predetermined `RecognitionResult` without network access or audio analysis. It is the default test seam for recognition integration tests and keeps CI deterministic.
