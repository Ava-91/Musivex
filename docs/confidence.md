# Confidence and review

Recognition confidence is bounded to `0..1`. Musivex should only auto-apply metadata when the configured threshold is met. Lower-confidence results remain reviewable and must not silently modify files.
