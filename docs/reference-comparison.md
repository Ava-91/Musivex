# Reference-track comparison

After recognition, Musivex can compare local audio features with a reference representation. The comparison layer exposes time-scale and similarity signals; transformation classifiers consume those signals rather than directly depending on a recognition provider.
