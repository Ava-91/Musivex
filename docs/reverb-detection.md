# Reverb detection

Musivex treats reverb as a probabilistic signal. The detector exposes a score and confidence rather than claiming that every naturally roomy recording is an added-reverb edit. Reference similarity is used as a guard before transformation classification.
