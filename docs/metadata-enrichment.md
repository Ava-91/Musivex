# Metadata enrichment

Recognition identifies a recording; metadata providers enrich that identity with release and track fields. Musivex keeps this separate from recognition so providers can be swapped and cached without changing the recognition pipeline.
