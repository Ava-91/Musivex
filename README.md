# Musivex

Automatic music metadata enrichment and transformation-aware song recognition.

## Development

Musivex uses a standard Python `src/` layout.

```bash
python -m pip install -e .
python -m pytest
musivex --help
```

## Status

Early development. Core scanning, recognition, metadata enrichment, and safe tagging are being built incrementally.
